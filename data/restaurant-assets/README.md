# Restaurant asset sources

## Wok-To-Walk-Assets.pptx

Local copy (not committed — file is ~200 MB): `Wok-To-Walk-Assets.pptx`

Source: Desktop `Wok To Walk Assets.pptx`.

**GitHub:** Files over 100 MB cannot be pushed to GitHub without [Git Large File Storage (LFS)](https://git-lfs.com/) or an external archive.

For dashboard use, prefer a small tracked file such as:

- `wok-to-walk-assets.json` — structured list exported from the deck, or  
- `wok-to-walk-assets.csv` — same data in spreadsheet form

Once those exist here, pages can load them like other static JSON in the repo.
