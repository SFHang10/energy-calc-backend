# ETL compliance fixes (2026-09)

Follow-up to `docs/etl-mentions-inventory-2026-09.md`.

### E. Consumer ETL hub expansion
- Expanded `etl-official-site.html` into the single market-facing ETL guide.
- Routes: `/greenways/etl` (+ aliases `etl-hub`, `etl-official`).
- Removed duplicate ETL three-card block from Sustainable References; note links to `/greenways/etl`.
- Module `etl-official-site` href → `/greenways/etl`.
